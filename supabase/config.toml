# Supabase project configuration.
# Run `supabase link --project-ref mygbxodwktbsrrevxcol` to link to the remote project.

[functions.profile]
verify_jwt = true

[functions.sync-profile]
verify_jwt = true

[functions.submit-run]
verify_jwt = true

[functions.leaderboard]
# Public endpoint — withSupabase({ auth: "publishable" }) handles auth.
verify_jwt = false
